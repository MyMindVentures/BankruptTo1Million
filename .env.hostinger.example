# Copy to .env.hostinger on the VPS and adjust these values only if needed.
TRAEFIK_NETWORK=traefik-public
TRAEFIK_HTTPS_ENTRYPOINT=websecure
TRAEFIK_CERT_RESOLVER=letsencrypt

GITHUB_OWNER=MyMindVentures
GITHUB_REPO=BankruptTo1Million
GITHUB_TOKEN=
IMPACT_CACHE_TTL_MS=900000
